export interface Pig {
  pid: string;
  breed: string;
}

export interface Person {
  name: string;
  phone: string;
}

export interface Location {
  longitude: number;
  latitude: number;
  location: string;
}
